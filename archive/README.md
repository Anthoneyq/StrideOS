# Archive

Superseded or duplicate exports kept for reference.

## `legacy-files/`

Early SQL migrations, RLS policies, and an older `stride-os.html` snapshot from before the repo was structured under `stride-os-repo/` and `supabase/migrations/`. Use those paths for active development.
